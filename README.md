# Auto-stop on silence — UI previews

Detached screenshot branch for PR #1. Two PNGs captured headlessly against
a local uvicorn at 1440×900, device-scale 2.

| state | file |
|-------|------|
| collapsed (default / disabled) | `auto-stop-collapsed.png` |
| expanded (user enabled) | `auto-stop-expanded.png` |

This branch shares no history with `main` — the screenshot workflow that
refreshes `images/*.png` on the main line does not touch this branch.
